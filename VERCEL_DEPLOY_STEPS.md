# Пошаговая инструкция: Деплой на Vercel

## Шаг 1: Подготовка проекта для GitHub

### 1.1. Создайте репозиторий на GitHub
1. Зайдите на [github.com](https://github.com)
2. Нажмите "+" → "New repository"
3. Название: `transmarine` (или любое другое)
4. Выберите "Public" или "Private"
5. НЕ добавляйте README, .gitignore или лицензию (у нас уже есть)
6. Нажмите "Create repository"

### 1.2. Загрузите проект на GitHub

Выполните в терминале (замените `YOUR_USERNAME` на ваш GitHub username):

```bash
cd /Users/alinagodm/Desktop/Transmarine

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш username)
git remote add origin https://github.com/YOUR_USERNAME/transmarine.git

# Загрузите на GitHub
git branch -M main
git push -u origin main
```

---

## Шаг 2: Подключение к Vercel

### 2.1. В Vercel Dashboard
1. Нажмите **"Add New..."** → **"Project"**
2. Вы увидите список ваших GitHub репозиториев
3. Найдите `transmarine` и нажмите **"Import"**

### 2.2. Настройки проекта

Vercel обычно автоматически определяет настройки, но проверьте:

- **Framework Preset:** `Vite` (или `Other`)
- **Root Directory:** `.` (точка, корень проекта)
- **Build Command:** `npm run build`
- **Output Directory:** `dist`
- **Install Command:** `npm install`

### 2.3. Environment Variables (если нужны)

Если у вас есть API ключи (например, для погоды), добавьте их:
- Нажмите "Environment Variables"
- Добавьте переменные (например, `VITE_WEATHER_API_KEY`)

### 2.4. Деплой

1. Нажмите **"Deploy"**
2. Подождите 1-2 минуты
3. Готово! 🎉

---

## Шаг 3: После деплоя

### 3.1. Ваш сайт будет доступен по адресу:
```
https://transmarine.vercel.app
```
(или другое имя, которое вы выберете)

### 3.2. Настройка домена (опционально)

1. В Vercel Dashboard → Settings → Domains
2. Добавьте свой домен (например, `transmarine.ru`)
3. Следуйте инструкциям для настройки DNS

### 3.3. Автоматические обновления

Теперь каждый раз, когда вы делаете `git push`, Vercel автоматически:
- Соберет проект
- Задеплоит новую версию
- Обновит сайт

---

## Альтернатива: Деплой без GitHub (через CLI)

Если не хотите использовать GitHub, можно задеплоить напрямую:

```bash
# Установите Vercel CLI
npm i -g vercel

# В папке проекта
cd /Users/alinagodm/Desktop/Transmarine

# Запустите деплой
vercel

# Следуйте инструкциям в терминале
```

---

## Проблемы?

### Ошибка при сборке:
- Проверьте, что все зависимости в `package.json`
- Убедитесь, что `npm run build` работает локально

### Изображения не загружаются:
- Проверьте, что все файлы в `public/images/` закоммичены
- Убедитесь, что пути начинаются с `/images/...`

### Роутинг не работает:
- Vercel автоматически настроит роутинг для SPA
- Если проблемы, добавьте `vercel.json` (см. ниже)

---

## Файл vercel.json (если нужен)

Создайте `vercel.json` в корне проекта:

```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

Но обычно Vercel определяет это автоматически для Vite проектов.
