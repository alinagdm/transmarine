# Инструкция по деплою TransMarine на хостинг

## Подготовка проекта

1. **Соберите проект:**
   ```bash
   npm run build
   ```
   После сборки все файлы будут в папке `dist/`

2. **Проверьте сборку локально:**
   ```bash
   npm run preview
   ```

---

## Варианты деплоя

### 🚀 Вариант 1: Vercel (Самый простой, рекомендуется)

1. Зайдите на [vercel.com](https://vercel.com)
2. Зарегистрируйтесь через GitHub
3. Нажмите "Add New Project"
4. Импортируйте ваш репозиторий или загрузите папку `dist`
5. Настройки:
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`
6. Нажмите "Deploy"
7. Готово! Vercel автоматически даст вам домен и SSL

**Или через CLI:**
```bash
npm i -g vercel
vercel
```

---

### 🌐 Вариант 2: Netlify

1. Зайдите на [netlify.com](https://netlify.com)
2. Зарегистрируйтесь
3. Перетащите папку `dist` в область "Deploy manually"
4. Или подключите GitHub репозиторий:
   - **Build command:** `npm run build`
   - **Publish directory:** `dist`
5. Нажмите "Deploy site"

**Или через CLI:**
```bash
npm i -g netlify-cli
netlify deploy --prod --dir=dist
```

---

### 📦 Вариант 3: GitHub Pages

1. Убедитесь, что проект в GitHub репозитории
2. В настройках репозитория включите GitHub Pages:
   - Settings → Pages
   - Source: GitHub Actions
3. Файл `.github/workflows/deploy.yml` уже создан
4. Сделайте commit и push:
   ```bash
   git add .
   git commit -m "Setup GitHub Pages"
   git push
   ```
5. После push автоматически запустится деплой
6. Сайт будет доступен по адресу: `https://your-username.github.io/your-repo-name/`

**Важно:** Если деплоите в подпапку, раскомментируйте `base` в `vite.config.ts`:
```typescript
base: '/your-repo-name/',
```

---

### 🖥️ Вариант 4: Обычный хостинг (через FTP)

1. Соберите проект: `npm run build`
2. Загрузите **все содержимое папки `dist`** на хостинг:
   - Через FTP клиент (FileZilla, Cyberduck)
   - Или через панель управления хостингом
3. Загрузите файлы в корневую папку сайта (обычно `public_html` или `www`)
4. Убедитесь, что файл `.htaccess` настроен для SPA (см. ниже)

**Создайте файл `.htaccess` в папке `dist`:**

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

Это нужно для правильной работы React Router.

---

## ⚠️ Важные моменты

1. **API ключи:** Если используете API ключи, добавьте их в переменные окружения на хостинге
2. **Пути к изображениям:** Все изображения должны быть в `public/images/` - они автоматически копируются в `dist/`
3. **Роутинг:** Для SPA нужна настройка сервера (см. `.htaccess` выше)
4. **Проверка:** После деплоя проверьте все страницы и изображения

---

## 🔧 Быстрая проверка перед деплоем

```bash
# 1. Соберите проект
npm run build

# 2. Проверьте локально
npm run preview

# 3. Проверьте размер папки dist
du -sh dist

# 4. Убедитесь, что все изображения на месте
ls -la dist/images/
```

---

## 📞 Нужна помощь?

Если возникли проблемы:
- Проверьте консоль браузера на ошибки
- Проверьте логи деплоя на хостинге
- Убедитесь, что все зависимости установлены
