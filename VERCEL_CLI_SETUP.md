# Настройка Vercel CLI для локального управления

## 🔑 Получение токена для CLI

### Шаг 1: Получите токен в Vercel Dashboard

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в **Settings** → **Tokens**
3. Нажмите **"Create Token"**
4. Дайте токену имя (например, "Local CLI")
5. Выберите срок действия (или "No Expiration")
6. Скопируйте токен (он показывается только один раз!)

### Шаг 2: Используйте токен в CLI

```bash
# Войдите с токеном
npx vercel login --token YOUR_TOKEN_HERE

# Или используйте токен напрямую в командах
npx vercel --token YOUR_TOKEN_HERE
```

### Шаг 3: Свяжите проект

```bash
cd /Users/alinagodm/Desktop/Transmarine
npx vercel link
```

При запросе выберите:
- **Set up and deploy?** → `Y`
- **Which scope?** → `alinas-projects-f7a25476`
- **Link to existing project?** → `Y`
- **What's the name of your project?** → `transmarine`

## 🚀 Альтернатива: Использование через переменную окружения

Можно установить токен как переменную окружения:

```bash
# Добавьте в ~/.zshrc или ~/.bash_profile
export VERCEL_TOKEN="your_token_here"

# Затем используйте CLI без --token
npx vercel
```

## ⚠️ Безопасность

**НЕ коммитьте токен в Git!**

Если используете переменную окружения, добавьте в `.gitignore`:
```
.env
.env.local
.vercel/token
```

## 📝 Полезные команды

```bash
# Проверить авторизацию
npx vercel whoami

# Посмотреть информацию о проекте
npx vercel inspect

# Задеплоить в production
npx vercel --prod

# Посмотреть логи
npx vercel logs
```

---

**Примечание:** Если CLI не работает из-за версии Node.js, используйте веб-интерфейс Vercel - он работает отлично и не требует CLI.
